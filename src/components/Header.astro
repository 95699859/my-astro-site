---
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<header>
  <nav>
    <a href="/">Главная</a>
    <a href="/about">О нас</a>
  </nav>
  <h1>{title}</h1>
  <ThemeToggle />
</header>

<style>
  nav {
    display: flex;
    gap: 1rem;
    padding: 1rem;
    background: var(--nav-bg);
  }
  h1 {
    color: var(--text-primary);
  }
</style>